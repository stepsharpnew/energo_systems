name: Docker Compose CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 🛎️ Клонирование репозитория
        uses: actions/checkout@v4

      - name: 🔍 Проверка всех secrets
        run: |
          echo "Проверка наличия secrets..."

          # Проверяем все необходимые secrets
          if [ -z "${{ secrets.SERVER_HOST }}" ]; then
            echo "❌ SERVER_HOST не установлен!"
            exit 1
          else
            echo "✅ SERVER_HOST установлен"
          fi

          if [ -z "${{ secrets.SERVER_USER }}" ]; then
            echo "❌ SERVER_USER не установлен!"
            exit 1
          else
            echo "✅ SERVER_USER установлен: ${{ secrets.SERVER_USER }}"
          fi

          if [ -z "${{ secrets.SERVER_SSH_KEY }}" ]; then
            echo "❌ SERVER_SSH_KEY не установлен или пустой!"
            echo "Проверьте, что secret называется именно SERVER_SSH_KEY"
            exit 1
          else
            echo "✅ SERVER_SSH_KEY установлен"
            
            # Проверяем начало ключа (без вывода всего содержимого)
            KEY_START=$(echo "${{ secrets.SERVER_SSH_KEY }}" | head -c 50)
            KEY_END=$(echo "${{ secrets.SERVER_SSH_KEY }}" | tail -c 50)
            
            echo "Начало ключа: $KEY_START"
            echo "Конец ключа: $KEY_END"
            
            # Проверяем формат
            if echo "${{ secrets.SERVER_SSH_KEY }}" | grep -q "BEGIN.*PRIVATE KEY"; then
              echo "✅ Формат ключа правильный (приватный ключ)"
            else
              echo "❌ Неверный формат! Должен начинаться с 'BEGIN ... PRIVATE KEY'"
              echo "Первые 100 символов: $(echo "${{ secrets.SERVER_SSH_KEY }}" | head -c 100)"
              exit 1
            fi
            
            # Проверяем длину (приватный ключ должен быть достаточно длинным)
            KEY_LENGTH=$(echo "${{ secrets.SERVER_SSH_KEY }}" | wc -c)
            echo "Длина ключа: $KEY_LENGTH символов"
            if [ "$KEY_LENGTH" -lt 500 ]; then
              echo "⚠️  Ключ слишком короткий! Возможно, скопирован не полностью."
            fi
          fi

          if [ -z "${{ secrets.SSH_PASSPHRASE }}" ]; then
            echo "⚠️  SSH_PASSPHRASE не установлен (это нормально, если ключ без пароля)"
          else
            echo "✅ SSH_PASSPHRASE установлен"
          fi

          echo ""
          echo "Все необходимые secrets проверены!"

      - name: 🚀 Деплой на сервер по SSH
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          passphrase: ${{ secrets.SSH_PASSPHRASE }}
          source: "."
          target: "/home/${{ secrets.SERVER_USER }}/app"

      - name: 🏃‍♂️ Перезапуск сервиса на сервере
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          passphrase: ${{ secrets.SSH_PASSPHRASE }}
          script: |
            cd /home/${{ secrets.SERVER_USER }}/app
            echo "=== Проверка файлов на сервере ==="
            ls -la
            echo ""
            echo "Проверка docker-compose.yml:"
            cat docker-compose.yml | head -20
            echo ""
            echo "Проверка nginx.conf:"
            cat nginx.conf
            echo ""
            echo "Проверка front/Dockerfile:"
            cat front/Dockerfile || echo "Файл не найден"
            echo ""
            echo "Остановка старых контейнеров..."
            sudo docker compose down || true
            echo "Запуск новых контейнеров..."
            timeout 300s sudo docker compose up -d --build
            echo "Ожидание запуска контейнеров..."
            sleep 15

            echo ""
            echo "=== Статус контейнеров ==="
            sudo docker compose ps

            echo ""
            echo "=== Логи frontend ==="
            sudo docker compose logs --tail=50 frontend || echo "Не удалось получить логи frontend"

            echo ""
            echo "=== Логи nginx ==="
            sudo docker compose logs --tail=50 nginx || echo "Не удалось получить логи nginx"

            echo ""
            echo "=== Проверка портов ==="
            sudo netstat -tlnp | grep -E ':(80|443|5173)' || sudo ss -tlnp | grep -E ':(80|443|5173)' || echo "Не удалось проверить порты"

            echo ""
            echo "=== Проверка доступности frontend ==="
            sudo docker compose exec -T frontend wget -qO- http://localhost:5173 | head -20 || echo "Frontend не отвечает на порту 5173"

            echo ""
            echo "=== Проверка доступности nginx ==="
            curl -I http://localhost:80 || echo "Nginx не отвечает на порту 80"

            echo ""
            echo "=== Проверка firewall (ufw) ==="
            sudo ufw status || echo "ufw не установлен или не активен"

            echo ""
            echo "=== Проверка firewall (iptables) ==="
            sudo iptables -L -n | grep -E '(80|443|5173)' || echo "Правила для портов не найдены"

            echo ""
            echo "=== Информация о сети ==="
            ip addr show | grep -E 'inet ' || echo "Не удалось получить информацию о сети"
